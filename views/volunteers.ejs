<!DOCTYPE html>
<html>
    <head>
        <title>
            Volunteers
        </title>
    </head>
    <body>
        <h1>Volunteers</h1>
        <p><a href="/"><button>Home</button></a></p>
        <% volunteers.forEach(function(o) { %>
            <li ><span><%= o.name %> <%= o.year %></span></li>
        <% }); %>
    </body>
</html>