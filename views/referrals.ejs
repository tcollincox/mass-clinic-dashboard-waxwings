<!DOCTYPE html>
<html>
    <head>
        <title>
            Referrals
        </title>
    </head>
    <body>
        <h1>Referrals</h1>
        <p><a href="/"><button>Home</button></a></p>
        <% referrals.forEach(function(o) { %>
            <li ><span><%= o.place %> <%= o.year %></span></li>
        <% }); %>
    </body>
</html>